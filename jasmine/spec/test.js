//loop through all feeds and make sure url is defined and is not empty
// var urls;
// for(i = 0; i < allFeeds.length - 1; i++){
//    urls = allFeeds[i].url;
// };
//
//loop through all feeds and make sure name is defines and is not empty
// var feedName;
// for(i = 0; i < allFeeds.length - 1; i++){
//    feedName = allFeeds[i].name;
// };
